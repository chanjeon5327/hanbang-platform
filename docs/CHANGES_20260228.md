# 변경 요약 (2026-02-28)

## 변경 요약 표

| # | 파일 | 변경 내용 |
|---|------|-----------|
| 1 | `app/api/home/popular/route.ts` | user_interests 전건 조회 제거 → RPC `get_popular_content_ids` 호출, Postgres group by + count, 동률 그룹 내 Fisher-Yates shuffle, limit 24, `dynamic = "force-dynamic"`, TODO materialized view |
| 2 | `app/api/chat/[productId]/route.ts` | POST에서 createAdminClient 제거 → createClient 사용, insert RLS 정책에 맡김, productId UUID 검증 유지 |
| 3 | `components/media/VideoThumb.tsx` | onTouchStart/onTouchEnd 제거, `window.matchMedia("(hover: hover)")`로 hover 재생은 데스크탑만, 모바일은 onClick play/pause |
| 4 | `app/api/home/deadline/route.ts` | `export const dynamic = "force-dynamic"` 추가 |
| 5 | `supabase/migrations/20260228_rpc_popular_content_ids.sql` | 신규: `get_popular_content_ids(p_limit)` RPC (50개 상한) |

## 의존성

- popular API는 `get_popular_content_ids` RPC 필요 → `supabase db push` 또는 마이그레이션 적용 필요
