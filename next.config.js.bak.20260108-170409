/** @type {import('next').NextConfig} */
const nextConfig = {
  // 타입 에러 때문에 dev/빌드가 막히는 걸 방지 (런칭 전에는 반드시 정리 필요)
  typescript: { ignoreBuildErrors: true },

  // 외부 이미지 허용
  images: {
    remotePatterns: [
      { protocol: "https", hostname: "images.unsplash.com", pathname: "**" },
    ],
  },

  // ✅ dev 서버에서 127.0.0.1 접속 시 _next 리소스 차단 방지
  allowedDevOrigins: [
    "http://localhost:3000",
    "http://127.0.0.1:3000",
  ],
};

module.exports = nextConfig;
